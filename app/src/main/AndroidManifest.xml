<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.hanb">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.HanB">
        <activity android:name=".WritePostscriptActivity"></activity>
        <activity android:name=".ProgramInfoActivity" />
        <activity android:name=".ProgramActivity" />
        <activity android:name=".ChangePasswordActivity" />
        <activity android:name=".ChangeNicknameActivity" />
        <activity android:name=".MypageActivity" />
        <activity android:name=".PostscriptActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".FindActivity" />
        <activity android:name=".SignupActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>